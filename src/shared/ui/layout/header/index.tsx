'use client';

import { LayoutHeader } from './layoutHeader';
import { NavLinks } from './navLinks';

export const Header = () => {
  return (
    <LayoutHeader>
      <NavLinks />
    </LayoutHeader>
  );
};
